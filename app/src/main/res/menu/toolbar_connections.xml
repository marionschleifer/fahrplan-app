<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools" tools:context=".ConnectionListActivity">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_favorite_connections"
        android:icon="@drawable/ic_favorite_border_black_48dp"
        android:title="@string/action_favorite"
        app:showAsAction="ifRoom"/>

    <!-- über uns, should always be in the overflow -->
    <item android:id="@+id/action_about_connections"
        android:title="@string/action_about"
        app:showAsAction="never"/>

</menu>